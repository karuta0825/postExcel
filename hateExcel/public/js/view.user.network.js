/**
 * ネットワークタブ
 */

( function ( $, cms ) {

  var
    networkView
  , initModule
  ;

  initModule = function () {

  };

  // to public
  cms.view.userNetwork = {
    initModule : initModule
  };

} ( jQuery, customer ));